# Deprecated entrypoint

**Created by Jayden Shutt.**

This folder is a **legacy wrapper** kept for old scripts. Use the package CLI:

```bash
pip install -e ..
python -m awstools cost --help
```

See the [root README](../README.md) and [docs/TEAMS.md](../docs/TEAMS.md).
