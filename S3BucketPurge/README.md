# Deprecated entrypoint

**Created by Jayden Shutt.**

This folder is a **legacy wrapper**. Use:

```bash
python -m awstools purge-s3 --help
```

Dry-run by default. Live deletes require `--execute --confirm-account`.

See the [root README](../README.md) and [docs/TEAMS.md](../docs/TEAMS.md).
